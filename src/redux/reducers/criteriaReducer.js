import {} from '../actions/actionTypes';

const initialState = {
  criterias: [] 
}

export default function(state = initialState, action) {
  switch (action.type) {
      default:
          return state
  }
}